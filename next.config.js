/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/MYREAL-AI-',
  assetPrefix: '/MYREAL-AI-/',
  output: 'export',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
